import { Components } from '@mui/material/styles/components';
import { Theme, ThemeOptions } from '@mui/material';

/**
 * Override global styles for Mui components.
 * @param themeOptions
 */
export const componentStyleOverrides = (themeOptions: ThemeOptions): Components<Theme> => {
    return {

    }
}
